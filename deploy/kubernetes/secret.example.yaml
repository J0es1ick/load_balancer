apiVersion: v1
kind: Secret
metadata:
  name: balancer-secrets
  namespace: load-balancer
type: Opaque
stringData:
  admin-token: REPLACE_WITH_A_LONG_RANDOM_VALUE
  redis-password: REPLACE_WITH_REDIS_CREDENTIAL
  postgres-password: REPLACE_WITH_POSTGRES_CREDENTIAL
